"""
    列表
    []表示列表   多个内容使用逗号分隔
    列表是有序集合。访问列表只要将元素的 位置 或 索引即可    列表名[索引] 索引从0开始   支持负数 索引-1开始
"""
# 定义一个列表。并打印列表
liebiao = ["zhangsan","lisi","wangwu"]
print(liebiao)  # 结果是将整个列表打印出来 包括 [] 也包含" " 号

# 访问列表
print(liebiao[0])  # 结果是 zhangsan  不包含[] 和 ""

